import {{action}}Command from '../../../Commands/Command/{{grouping}}/{{action}}Command';
import { injectable, inject } from 'inversify';

@injectable()
class {{action}}Handler
{
    /**
     * Execute the given command
     *
     * @return void
     */
    public async execute(command: {{action}}Command): Promise<void>
    {

    }
}

export default {{action}}Handler;